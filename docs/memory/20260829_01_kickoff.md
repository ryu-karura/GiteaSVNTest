# 20260829_01 キックオフ

## 決定した作業項目（順番）

1. 環境変数 命名標準化 → `docs/env-vars.md`
2. Docker Compose 定義（Docker0-6）→ `infra/`
3. 初期データ投入（テストリポジトリ、Redmine プロジェクト、サンプルチケット）
4. 疎通ヘルスチェック手順 → `docs/healthcheck.md`
5. AI 指示例（共通要件 + Gitea PR / Redmine / Git / SVN の curl・コマンド手順）→ `ai-instructions/`
6. Claude 定義ファイル → `.claude/`（skills / rules）
7. Copilot 定義ファイル → `.github/`（copilot-instructions / skills）
8. メモリ運用（本ディレクトリ）

## 確認事項の回答（README 末尾 3 件）

1. 環境変数名: **標準化して定義してよい**
2. Docker 定義: **初期データ投入まで含める**（seed/init スクリプト同梱、`compose up` で試験可能な状態）
3. Copilot 用と Claude 用: **完全に別々**に個別最適化（共通資産中心にしない）

## 試験環境（README より）

- Docker0: Cockpit（Docker1-N Manager）
- Docker1: Redmine 6.1.2
- Docker2: RedmineDB（MariaDB）
- Docker3: SVN + Apache Repository
- Docker4: Git + Gitea（Postgres）+ Apache Repository
- Docker5: SVN + Apache Repository
- Docker6: Action Only（Gitea-runner）

## 制約

- PR 作成は `tea` / `gh` コマンド禁止。**curl のみ**。
- Redmine 操作も curl。
- SVN / Git 操作は標準コマンド。
- 認証情報は環境変数から参照。リポジトリ配下に置かない。
