# 20260830_01 前日分の集約と本日の作業計画

2026-08-29 の作業メモリ（`20260829_01`〜`20260829_11`）を 1 ファイルに集約する。
個別ファイルは履歴として残し（削除しない）、`index.md` の番号は本ファイルから振り直す。

## 2026-08-29 までに完了したこと

- **環境変数の標準化**（`docs/env-vars.md`）: Gitea / GitHub / Redmine / SVN / Git の
  変数名を `<service>_<purpose>` 大文字で統一。未設定チェックスクリプト同梱。
- **疎通ヘルスチェック**（`docs/healthcheck.md`）: AI 経路での Gitea / Redmine / SVN / Git
  到達確認と終了条件。冒頭に RTK 注記。
- **試験環境**（`infra/`）: podman rootless で 8 コンテナ稼働。
  cockpit / redmine / redmine-db / gitea / gitea-db / svn1 / svn2 / gitea-runner。
  `git-apache` は削除（Git は Gitea エンドポイントに一本化）。
  初期データ投入は `seed`（冪等）+ `redmine_bootstrap.rb`。
- **AI 手順集**（`ai-instructions/` 7 ファイル）: ツール非依存。curl / git / svn の
  レシピ本文は `rtk` 無しで記載（可搬性）。「rtk があれば前置」は `common.md` に集約。
- **AI 定義ファイル**: `.claude/`（rules + skills 3 + `CLAUDE.md` 追記）と
  `.github/`（copilot-instructions + instructions + skills 3）を個別記述。
- **ドキュメント再編**: 旧 `README.md` → `PLAN.md`。新 `README.md`（現状サマリ）+
  `MANUAL.md`（構築〜AI 実行の通し手順）を追加。MANUAL は clean state から通しテスト済み（全成功）。
- **検証済み**:
  - E2E: curl で Gitea PR 作成（201）→ Redmine 課題 PUT（204、status→In Progress）→ 反映確認。
  - SVN: ホストから checkout / add / edit / commit（r3）/ `svn copy` ブランチ（r4）/ list。
  - git push: `${GITEA_BASE_URL}/${GITEA_OWNER}/${GITEA_REPO}.git` へ credential.helper 経由で成功。
  - Gitea Actions runner: `gitea/runner:3.3.1` で登録成功（v3.3.1、labels [ubuntu-latest]、daemon 稼働）。
    `.gitea/workflows/ci.yml` を push → runner がジョブ受信。
- **RTK**: git / curl は Claude Code フックが自動で `rtk` 化（svn は対象外）。
  `rtk curl` は jq パースを壊さない。各指示ファイルに「rtk があれば前置」を明記。
- **PR**: GitHub PR #1（feat/ai-test-scaffold ではなく seed 検証時のもの、Gitea 上）、
  GitHub PR #2（feat/ai-test-scaffold）、**GitHub PR #3**（feat/gitea-actions-runner、
  https://github.com/ryu-karura/GiteaSVNTest/pull/3 、**オープン中・未マージ**）。

## 既知の未解決・未検証（本日以降の候補）

- **B**: rootless podman で Gitea Actions ジョブが完走しない
  （`NewParallelExecutor: ... 0 executors` ループ、docker.sock の uid / 権限不一致）。
  → `gitea/runner:3.3.1-dind-rootless` + DinD、または通常 Docker が必要。
- **C**: GitHub PR 手順（`github-pr.md`）の実リポジトリ通し検証（API 単体は実証済み、手順 E2E 未）。
- **D**: cockpit の実動作確認（Docker 管理 UI としての操作。現状は起動のみ）。
- **E**: Gitea Actions のワークフロー結果（成功/失敗）取得手順の整備
  （`actions/tasks` API は 404、`gitea actions runners list` サブコマンドも無し。
  確認は runner ログか管理画面のみ）。
- **F**: 監査ログ・操作履歴の残し方（`PLAN.md` の追加検討事項、未着手）。
- **A**: PR #3 のマージ（ユーザー操作）とマージ後の main 同期 / ブランチ削除。

## 本日（2026-08-30）の予定順

1. **本メモリ整理**（このファイル作成 + `index.md` 番号振り直し）。← 着手中
2. **B — Actions ジョブ完走**: `gitea/runner:3.3.1-dind-rootless` + DinD に切替 →
   `ci.yml` 再 push → ジョブ成功まで確認 → `MANUAL.md` / `docker-compose.yml` に反映。
3. **E — ワークフロー結果取得手順**: B の副産物として、runner ログ + Gitea Web / API での
   成否確認方法を `ai-instructions/` に追記。
4. 余力に応じて **C**（GitHub PR 手順 E2E）→ **D** / **F**。
